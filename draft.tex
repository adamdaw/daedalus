% Draft watermark — included via -H when DRAFT=1 is passed to make build.
% Requires the draftwatermark package (included in texlive-latex-extra).
\usepackage{draftwatermark}
\SetWatermarkText{DRAFT}
\SetWatermarkScale{1}
\SetWatermarkColor[gray]{0.88}
\SetWatermarkAngle{45}
